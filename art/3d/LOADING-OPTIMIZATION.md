# 森林启动优化

旧树根排除步骤：每棵草访问25个周边桶，对桶里的树重复求道路朝向，即使已确定遮挡仍继续遍历。实测约3.2秒。

改为树脚占地索引：树的朝向和覆盖格只计算一次，每棵草只查询所在32单位格，命中后立即结束。保留同一占地判定及原有根部补草。不是取消必要的穿插约束。

固定种子/幽暗密林参数验证：13,873个筛选后基础物件ID与顺序完全一致；实际路段仍7531个物件。首帧从约4.61秒降至1.36秒（同机开发运行，非冷启动保证）。动作预览库不在实战加载路径中。
