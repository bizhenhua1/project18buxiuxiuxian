16:9 character-select / bestiary collage, NOT a story illustration.

Plain flat solid background only. Isolated subjects arranged in one horizontal row (or left hero, center artifacts in a row, right monsters in a row) with generous empty space between each subject.

Each subject fully visible as its own cutout: one character, each treasure, each monster separately. Never overlapping, never wrapping together, never sharing a ground shadow that unites them.

Optional: a very small simple gold circular disc graphic behind the MAIN character's head only as decoration — not a divine nimbus, no radiating rays. Prefer omitting it for Fengshen / Daoist production sheets.

No environment between the figures; the gaps must stay empty flat color.
