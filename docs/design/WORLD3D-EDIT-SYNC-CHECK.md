# 镜头与站位编辑接入复核

2026-09-14，森林标准切片，未改写用户的正式编辑文件。

独立 3D 舞台继续轮询传统镜头编辑器的有效快照，机位平滑更新。修复等价角度（相差整圈）导致过渡永不完成的问题：收敛使用最短角度差，收敛后采用保存值本身。普通数值也在收敛后精确采用保存快照，避免保留无限小尾差。无须更改已保存角度。

运行 test_world3d_live_camera.gd：镜头平滑更新、世界站位不变、整圈角度不转动且正常结束、写入不完整快照时保留最后有效值，均通过。

运行 test_world3d_live_formation.gd：新站位在下一事件进入战斗时采用，当前战斗重开仍保留站位；应用新目标不瞬移、不瞬间缩放，角色与道具尺寸随后正确到位，均通过。这是现有生效时机，不能把它描述为站位保存后立刻打断当前战斗重新排阵。

本次是保存接入和状态正确性验证，不代替所有编辑参数组合的视觉验收，也未进行性能专项。
