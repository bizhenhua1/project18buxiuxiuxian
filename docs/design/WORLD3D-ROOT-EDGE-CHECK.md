# 森林树根硬切边修正

2026-09-14。固定离场截图左侧硬边定位到 `assets/style2/tree-b.png`，其原图树干在图片底部截平。实例已使用自适应接地模式；仅校准 root 不会消除资产自身的截平轮廓。

在自适应接地的主体网格中，按顶点到局部地面的高度计算六厘米接触透明过渡。复用现有材质与透明通道，不增加贴图、草丛、几何或绘制层；根部覆盖附件和普通直立草木不应用这个过渡，原图及传统版不变。主体位置、尺寸、上部树干轮廓不变。

固定七检查点 GPU 流程完成，无脚本/着色器错误，镜头缓停断言仍通过；查看离场截图，左侧原有水平硬边被短接地过渡替代，背后的根部草木可正常显露。该效果属于边缘融合，不生成缺失的树根；不能据此认定所有穿插、悬浮或其他主题均已解决。未追加性能压测，未声称零开销。

复现：运行 `capture_world3d_presentation_flow.gd -- --inspect-roots`，可额外记录左侧候选实例的源资产、位置、尺寸与锚点到 `tempassets/work/world3d-departure-roots.json`。

## 大树近景补充

自然战斗离场截图仍能辨认树干底部的平切形状。接触过渡现改为树宽的 2.5%，限制在 6–18 厘米，并用固定在图片横坐标上的小幅变化打散直线边缘。仅影响原有自适应主体的根部透明度，没有新增实例、几何、贴图或绘制层；片元运算有所增加，不宣称零成本。

运行普通遭遇 GPU 截图检查通过，查看更新后的 world3d-natural-departure.png：边缘融合范围增加，但原图缺失真实树根的问题仍存在。这是有限修正，不能靠继续加宽透明带替代资产修复，否则会制造树干悬浮感。后续优先处理更影响玩法和整体复原的内容，不在这里无限试调渐隐参数。
