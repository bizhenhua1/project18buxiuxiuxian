# 保存队伍接入修正

2026-09-14：独立 3D 的队伍读取此前固定 stage=2，未按已保存世界 map_index 恢复成长阶段；现在与 Journey 的阶段规则一致，map_index+2，上限 31。没有写回用户存档。

同时修复手持模式定位：BattleModel.add_card 会把术式排到最后，不能将 player.size()-1 当成刚加入的道具。现在记录加入前的单位身份，准确找到新单位并恢复 held 状态，保留卡牌排序规则。

合成存档覆盖第七张地图、两个不同模型角色、提前插入术式后加入手持封印物。成长阶段、血量/攻击/冷却与同阶段 BattleModel 一致，手持模式正确，输入存档未变。实际 Stage 的保存装备同步回归通过：装备与战斗职业一致、重复整备复用附件、镜头与位置不变。

这不代表全部整备 UI 已迁移；当前入口仍主要在加载时读取保存队伍，装备在整备与开战时同步。
