[
  {"wait": 3200},
  {"eval": "(() => { const ks = []; for (let i = 0; i < localStorage.length; i++) { const k = localStorage.key(i); if (k && k.startsWith('dao-')) ks.push(k); } ks.forEach(k => localStorage.removeItem(k)); location.reload(); return 'cleared'; })()"},
  {"wait": 3200},
  {"eval": "(() => { const p = window.__dao; p.clear(); p.place('waci-yin'); const u = p.state.playerQueue.find(x=>x.cardId==='waci-yin'); u.maxHp = 4000; u.hp = 4000; const r1 = p.hit(u.uid, 100, 'phys'); const nat = Math.round(100 * (1 - 6/(6+60))); u.physDef = 90; u.spellDef = 0; u.dmgReduce = 0; const r2 = p.hit(u.uid, 100, 'phys'); const r3 = p.hit(u.uid, 100, 'spell'); u.dmgReduce = 0.3; const r4 = p.hit(u.uid, 100, 'phys'); u.dmgReduce = 0; u.physDef = 100000; const r5 = p.hit(u.uid, 100, 'phys'); return JSON.stringify({ stageSnap: u.stageSnap, natural: r1.dealt, naturalOk: r1.dealt === nat && nat === 91, physHiDef: r2.dealt, physHiOk: r2.dealt === 40, spellVsZeroSpellDef: r3.dealt, spellOk: r3.dealt === 100, multiplied: r4.dealt, multOk: r4.dealt === 28, capped: r5.dealt, capOk: r5.dealt === 25 }); })()"},
  {"shot": "at10_damage_formula.png"}
]
