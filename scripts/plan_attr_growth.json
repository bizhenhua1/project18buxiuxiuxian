[
  {"wait": 3200},
  {"eval": "(() => { const ks = []; for (let i = 0; i < localStorage.length; i++) { const k = localStorage.key(i); if (k && k.startsWith('dao-')) ks.push(k); } ks.forEach(k => localStorage.removeItem(k)); location.reload(); return 'cleared'; })()"},
  {"wait": 3200},
  {"eval": "(() => { const p = window.__dao; p.setStage(0); const e = p.state.enemyQueue[0]; e.maxHp = 99999; e.hp = 99999; const r = p.hit(e.uid, 100, 'phys'); const expected = Math.round(100 * (1 - Math.min(0.75, e.physDef/(e.physDef+60)))); return JSON.stringify({ stage: 0, id: e.cardId, physDef: e.physDef, spellDef: e.spellDef, defAddOk: e.physDef === (e.basePhysDef||0) && e.spellDef === (e.baseSpellDef||0), dealt: r.dealt, dealtOk: r.dealt === expected, critChance: e.critChance }); })()"},
  {"eval": "(() => { const p = window.__dao; p.setStage(12); const s = 12; const add = Math.round(s*0.8); const q = p.state.enemyQueue; const defOk = q.every(u => u.physDef === (u.basePhysDef||0) + add && u.spellDef === (u.baseSpellDef||0) + add); const grow = Math.min(0.10, s*0.008); const atkOk = q.every(u => u.atk === Math.max(1, Math.round(Math.max(1, Math.round(u.baseAtk*u.atkMult)) * (1+grow)))); const e = q[0]; e.maxHp = 99999; e.hp = 99999; const r = p.hit(e.uid, 100, 'phys'); const expected = Math.round(100 * (1 - Math.min(0.75, e.physDef/(e.physDef+60+4*s)))); return JSON.stringify({ stage: s, id: e.cardId, physDef: e.physDef, base: e.basePhysDef, defAdd: add, defOk, atkGrowOk: atkOk, dealt: r.dealt, dealtOk: r.dealt === expected, damageDropped: r.dealt < 100, bossCrit: e.critChance }); })()"},
  {"eval": "(() => { const p = window.__dao; p.setStage(7); const crits = p.state.enemyQueue.map(u => u.critChance); const ok7 = crits.every(c => c === 0.1); p.setStage(4); const crits4 = p.state.enemyQueue.map(u => u.critChance); const ok4 = crits4.every(c => c === 0); return JSON.stringify({ bossStage7AllCrit10: ok7, normalStage4NoCrit: ok4, crits, crits4 }); })()"},
  {"shot": "at40_enemy_growth.png"}
]
