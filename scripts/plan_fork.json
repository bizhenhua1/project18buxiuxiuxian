[
  {"wait": 4200},
  {"eval": "(async () => { await window.__corridorSetTheme('cave'); window.__corridorFork(3); const t0 = Date.now(); while (Date.now() - t0 < 10000) { const s = window.__corridorSnap(); if (s && s.fork === 'choosing') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = 'hidden'; return !!p; })()"},
  {"shot": "fork-demo-cave-geometry.png"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = ''; return 'shown'; })()"},
  {"shot": "fork-demo-cave-choosing.png"},
  {"eval": "(() => { const b = document.querySelector('.corridor-fork-panel button:nth-child(2)') || document.querySelector('.corridor-fork-panel button'); if (!b) return 'no-btn'; b.click(); return b.textContent; })()"},
  {"eval": "(async () => { const t0 = Date.now(); while (Date.now() - t0 < 9000) { const s = window.__corridorSnap(); if (s && s.fork === 'hidden') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"wait": 400},
  {"shot": "fork-demo-cave-after.png"},
  {"eval": "(async () => { await window.__corridorSetTheme('forest'); window.__corridorFork(3); const t0 = Date.now(); while (Date.now() - t0 < 10000) { const s = window.__corridorSnap(); if (s && s.fork === 'choosing') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"shot": "fork-demo-forest-choosing.png"},
  {"eval": "(async () => { const b = document.querySelector('.corridor-fork-panel button'); if (b) b.click(); const t0 = Date.now(); while (Date.now() - t0 < 9000) { const s = window.__corridorSnap(); if (s && s.fork === 'hidden') return s; await new Promise((r) => setTimeout(r, 80)); } await window.__corridorSetTheme('cloudsea'); window.__corridorFork(2); const t1 = Date.now(); while (Date.now() - t1 < 10000) { const s = window.__corridorSnap(); if (s && s.fork === 'choosing') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"shot": "fork-demo-cloudsea-choosing.png"},
  {"eval": "({drawMs: window.__corridorDrawMs, snap: window.__corridorSnap()})"}
]
