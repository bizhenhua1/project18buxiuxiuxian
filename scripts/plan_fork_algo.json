[
  {"wait": 4200},
  {"eval": "(async () => { await window.__corridorSetTheme('forest'); window.__corridorSetHour(11); window.__corridorFork(3); return window.__corridorSnap(); })()"},
  {"eval": "(async () => { const t0 = Date.now(); while (Date.now() - t0 < 25000) { const s = window.__corridorSnap(); if (s.fork === 'choosing' || (s.fork === 'approaching' && s.pauseZ - s.camZ < 480)) return s; await new Promise((r) => setTimeout(r, 60)); } return window.__corridorSnap(); })()"},
  {"shot": "fork2-forest-approach.png"},
  {"eval": "(async () => { const t0 = Date.now(); while (Date.now() - t0 < 25000) { const s = window.__corridorSnap(); if (s.fork === 'choosing') return s; await new Promise((r) => setTimeout(r, 60)); } return window.__corridorSnap(); })()"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = 'hidden'; return !!p; })()"},
  {"shot": "fork2-forest-choosing-clean.png"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = ''; return 'shown'; })()"},
  {"shot": "fork2-forest-choosing.png"},
  {"eval": "(() => { const b = document.querySelector('.corridor-fork-panel button'); if (!b) return 'no-btn'; b.click(); return b.textContent; })()"},
  {"eval": "(async () => { const p0 = window.__corridorSnap().pauseZ; const t0 = Date.now(); while (Date.now() - t0 < 25000) { const s = window.__corridorSnap(); if (s.fork !== 'committing' || s.camZ - p0 > 560) return s; await new Promise((r) => setTimeout(r, 60)); } return window.__corridorSnap(); })()"},
  {"shot": "fork2-forest-commit-mid.png"},
  {"eval": "(async () => { const t0 = Date.now(); while (Date.now() - t0 < 25000) { const s = window.__corridorSnap(); if (s.fork === 'hidden') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"wait": 500},
  {"shot": "fork2-forest-after.png"},
  {"eval": "(async () => { await window.__corridorSetTheme('cave'); window.__corridorFork(2); const t0 = Date.now(); while (Date.now() - t0 < 25000) { const s = window.__corridorSnap(); if (s.fork === 'choosing') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = 'hidden'; return !!p; })()"},
  {"shot": "fork2-cave-choosing-clean.png"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = ''; return 'shown'; })()"},
  {"eval": "(() => { const bs = document.querySelectorAll('.corridor-fork-panel button'); const b = bs[bs.length - 1]; if (!b) return 'no-btn'; b.click(); return b.textContent; })()"},
  {"eval": "(async () => { const p0 = window.__corridorSnap().pauseZ; const t0 = Date.now(); while (Date.now() - t0 < 25000) { const s = window.__corridorSnap(); if (s.fork !== 'committing' || s.camZ - p0 > 560) return s; await new Promise((r) => setTimeout(r, 60)); } return window.__corridorSnap(); })()"},
  {"shot": "fork2-cave-commit-mid.png"},
  {"eval": "(async () => { const t0 = Date.now(); while (Date.now() - t0 < 25000) { const s = window.__corridorSnap(); if (s.fork === 'hidden') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"wait": 500},
  {"shot": "fork2-cave-after.png"},
  {"eval": "({drawMs: window.__corridorDrawMs, snap: window.__corridorSnap()})"}
]
