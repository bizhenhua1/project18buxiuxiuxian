[
  {"wait": 4500},
  {"eval": "(async () => { if (window.__corridorSetTheme) await window.__corridorSetTheme('forest'); window.__corridorSetHour && window.__corridorSetHour(11); window.__dao.fork(3); const t0 = Date.now(); while (Date.now() - t0 < 18000) { const s = window.__corridorSnap(); if (s && s.fork === 'choosing') return s; await new Promise((r) => setTimeout(r, 80)); } return { snap: window.__corridorSnap() }; })()"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = 'hidden'; return !!p; })()"},
  {"shot": "fork3-band-forest-choosing-clean.png"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = ''; const b = document.querySelector('.corridor-fork-panel button'); if (!b) return 'no-btn'; b.click(); return b.textContent; })()"},
  {"eval": "(async () => { const s0 = window.__corridorSnap(); const spanEnd = s0.joinZ + 1250; const t0 = Date.now(); while (Date.now() - t0 < 18000) { const s = window.__corridorSnap(); if (s.fork !== 'committing' || s.camZ > spanEnd - 50) return s; await new Promise((r) => setTimeout(r, 40)); } return window.__corridorSnap(); })()"},
  {"shot": "pop-seq-1-before.png"},
  {"wait": 200},
  {"shot": "pop-seq-2-before.png"},
  {"eval": "(async () => { const t0 = Date.now(); while (Date.now() - t0 < 18000) { const s = window.__corridorSnap(); if (s.fork === 'hidden') return s; await new Promise((r) => setTimeout(r, 30)); } return window.__corridorSnap(); })()"},
  {"shot": "pop-seq-3-after-dismantle.png"},
  {"wait": 200},
  {"shot": "pop-seq-4-after-dismantle.png"},
  {"eval": "(async () => { const t0 = Date.now(); while (Date.now() - t0 < 18000) { const s = window.__corridorSnap(); if (!s.traveling) return s; await new Promise((r) => setTimeout(r, 60)); } return window.__corridorSnap(); })()"},
  {"wait": 400},
  {"shot": "pop-seq-5-encounter-stop.png"},
  {"eval": "(async () => { await window.__corridorSetTheme('cave'); window.__dao.fork(3); const t0 = Date.now(); while (Date.now() - t0 < 18000) { const s = window.__corridorSnap(); if (s && s.fork === 'choosing') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = 'hidden'; return !!p; })()"},
  {"shot": "fork3-band-cave3-choosing-clean.png"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = ''; const bs = document.querySelectorAll('.corridor-fork-panel button'); const b = bs[1]; if (b) b.click(); return b ? b.textContent : 'no-btn'; })()"},
  {"eval": "(async () => { const t0 = Date.now(); while (Date.now() - t0 < 18000) { const s = window.__corridorSnap(); if (s.fork === 'hidden' && !s.traveling) return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"eval": "(async () => { window.__dao.fork(2); const t0 = Date.now(); while (Date.now() - t0 < 18000) { const s = window.__corridorSnap(); if (s && s.fork === 'choosing') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = 'hidden'; return !!p; })()"},
  {"shot": "fork3-band-cave-choosing-clean.png"},
  {"eval": "({drawMs: window.__corridorDrawMs, snap: window.__corridorSnap()})"}
]
