[
  {"wait": 4500},
  {"eval": "(async () => { if (window.__corridorSetTheme) await window.__corridorSetTheme('cave'); const p = window.__dao && window.__dao.fork(3); const t0 = Date.now(); while (Date.now() - t0 < 10000) { const s = window.__corridorSnap(); if (s && s.fork === 'choosing') return s; await new Promise((r) => setTimeout(r, 80)); } return { snap: window.__corridorSnap(), dao: !!window.__dao }; })()"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = 'hidden'; return !!p; })()"},
  {"shot": "fork-index-geometry.png"},
  {"eval": "(() => { const p = document.querySelector('.corridor-fork-panel'); if (p) p.style.visibility = ''; return 'shown'; })()"},
  {"shot": "fork-index-choosing.png"},
  {"eval": "(() => { const b = document.querySelector('.corridor-fork-panel button'); if (!b) return 'no-btn'; b.click(); return b.textContent; })()"},
  {"eval": "(async () => { const t0 = Date.now(); while (Date.now() - t0 < 10000) { const s = window.__corridorSnap(); if (s && s.fork === 'hidden') return s; await new Promise((r) => setTimeout(r, 80)); } return window.__corridorSnap(); })()"},
  {"wait": 500},
  {"shot": "fork-index-after.png"}
]
